#include "main.h"

/**
  * _abs- Function name
  * Description: Compute the absolute value of an integer.
  * @n: parameter passed to the function for checking.
  * Return: Return the absolute value int n.
  */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
